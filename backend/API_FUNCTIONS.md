# Список API функций для магазина обуви

Документация всех созданных контроллеров и их функций для бэкенда магазина обуви.

## 🔐 Аутентификация (Auth)

**Модуль:** `auth`

### POST `/auth/register`
- **Описание:** Регистрация нового пользователя
- **Требует аутентификации:** Нет
- **Параметры:**
  - `username` (string) - Имя пользователя
  - `email` (string) - Email
  - `password` (string) - Пароль (минимум 6 символов)
  - `phone` (string, опционально) - Телефон
  - `address` (string, опционально) - Адрес
- **Возвращает:** JWT токен

### POST `/auth/login`
- **Описание:** Вход в систему
- **Требует аутентификации:** Нет
- **Параметры:**
  - `email` (string) - Email
  - `password` (string) - Пароль
- **Возвращает:** JWT токен

---

## 👤 Пользователи (Users)

**Модуль:** `users`

### GET `/users/me`
- **Описание:** Получить профиль текущего пользователя
- **Требует аутентификации:** Да
- **Возвращает:** Информация о пользователе (id, email, role, avatar)

### PATCH `/users/me/avatar`
- **Описание:** Обновить аватар пользователя
- **Требует аутентификации:** Да
- **Параметры:**
  - `avatar` (string) - URL аватара
- **Возвращает:** Обновленный профиль пользователя

### GET `/users/:id`
- **Описание:** Получить информацию о пользователе по ID
- **Требует аутентификации:** Нет
- **Параметры:**
  - `id` (UUID) - ID пользователя
- **Возвращает:** Информация о пользователе

---

## 📦 Товары (Products)

**Модуль:** `products`

### GET `/products`
- **Описание:** Получить список всех товаров
- **Требует аутентификации:** Нет
- **Возвращает:** Массив товаров с категориями

### GET `/products/:id`
- **Описание:** Получить информацию о товаре по ID
- **Требует аутентификации:** Нет
- **Параметры:**
  - `id` (UUID) - ID товара
- **Возвращает:** Информация о товаре

### POST `/products`
- **Описание:** Создать новый товар
- **Требует аутентификации:** Да (только администратор)
- **Параметры:**
  - `name` (string) - Наименование товара
  - `description` (string, опционально) - Описание
  - `price` (number) - Цена
  - `discount` (number, опционально) - Скидка
  - `image` (string, опционально) - Ссылка на изображение
  - `quantityInStock` (number, опционально) - Количество на складе
  - `productStatus` (boolean, опционально) - Статус товара
  - `categoryId` (UUID, опционально) - ID категории
- **Возвращает:** Созданный товар

### PATCH `/products/:id`
- **Описание:** Обновить товар
- **Требует аутентификации:** Да (только администратор)
- **Параметры:**
  - `id` (UUID) - ID товара
  - Тело запроса: поля для обновления
- **Возвращает:** Обновленный товар

### PATCH `/products/:id/image`
- **Описание:** Обновить изображение товара
- **Требует аутентификации:** Да (только администратор)
- **Параметры:**
  - `id` (UUID) - ID товара
  - `image` (string) - URL изображения
- **Возвращает:** Обновленный товар

### DELETE `/products/:id`
- **Описание:** Удалить товар
- **Требует аутентификации:** Да (только администратор)
- **Параметры:**
  - `id` (UUID) - ID товара
- **Возвращает:** Подтверждение удаления

---

## 📁 Категории товаров (Categories)

**Модуль:** `categories`

### GET `/categories`
- **Описание:** Получить список всех категорий с возможностью фильтрации
- **Требует аутентификации:** Нет
- **Query параметры (опционально):**
  - `material` (string) - Тип материала
  - `size` (number) - Размер
  - `season` (string) - Сезон
  - `style` (string) - Стиль
- **Возвращает:** Массив категорий с товарами

### GET `/categories/:id`
- **Описание:** Получить информацию о категории по ID
- **Требует аутентификации:** Нет
- **Параметры:**
  - `id` (UUID) - ID категории
- **Возвращает:** Информация о категории с товарами

### POST `/categories`
- **Описание:** Создать новую категорию
- **Требует аутентификации:** Да (только администратор)
- **Параметры:**
  - `material` (enum) - Тип материала (Натуральная кожа, Искусственная кожа, Замша, Нубук)
  - `size` (number) - Размер
  - `season` (string) - Сезон
  - `style` (enum) - Стиль (Классика, Спорт)
- **Возвращает:** Созданная категория

### PATCH `/categories/:id`
- **Описание:** Обновить категорию
- **Требует аутентификации:** Да (только администратор)
- **Параметры:**
  - `id` (UUID) - ID категории
  - Тело запроса: поля для обновления
- **Возвращает:** Обновленная категория

### DELETE `/categories/:id`
- **Описание:** Удалить категорию
- **Требует аутентификации:** Да (только администратор)
- **Параметры:**
  - `id` (UUID) - ID категории
- **Возвращает:** Подтверждение удаления

---

## 🛒 Корзина (Cart)

**Модуль:** `cart`

### GET `/cart`
- **Описание:** Получить содержимое корзины текущего пользователя
- **Требует аутентификации:** Да
- **Возвращает:** Массив элементов корзины с информацией о товарах

### POST `/cart`
- **Описание:** Добавить товар в корзину
- **Требует аутентификации:** Да
- **Параметры:**
  - `productId` (UUID) - ID товара
  - `quantity` (number) - Количество (минимум 1)
- **Возвращает:** Элемент корзины
- **Особенности:** Если товар уже в корзине, увеличивает количество

### PATCH `/cart/:id`
- **Описание:** Обновить количество товара в корзине
- **Требует аутентификации:** Да
- **Параметры:**
  - `id` (UUID) - ID элемента корзины
  - `quantity` (number) - Новое количество (минимум 1)
- **Возвращает:** Обновленный элемент корзины

### DELETE `/cart/:id`
- **Описание:** Удалить товар из корзины
- **Требует аутентификации:** Да
- **Параметры:**
  - `id` (UUID) - ID элемента корзины
- **Возвращает:** Подтверждение удаления

### DELETE `/cart`
- **Описание:** Очистить всю корзину
- **Требует аутентификации:** Да
- **Возвращает:** Подтверждение очистки

---

## ❤️ Избранное (Favorites)

**Модуль:** `favorites`

### GET `/favorites`
- **Описание:** Получить список избранных товаров текущего пользователя
- **Требует аутентификации:** Да
- **Возвращает:** Массив избранных товаров

### POST `/favorites/:productId`
- **Описание:** Добавить товар в избранное
- **Требует аутентификации:** Да
- **Параметры:**
  - `productId` (UUID) - ID товара
- **Возвращает:** Элемент избранного
- **Особенности:** Если товар уже в избранном, возвращает ошибку

### DELETE `/favorites/:productId`
- **Описание:** Удалить товар из избранного
- **Требует аутентификации:** Да
- **Параметры:**
  - `productId` (UUID) - ID товара
- **Возвращает:** Подтверждение удаления

### GET `/favorites/check/:productId`
- **Описание:** Проверить, находится ли товар в избранном
- **Требует аутентификации:** Да
- **Параметры:**
  - `productId` (UUID) - ID товара
- **Возвращает:** Boolean значение

---

## 📋 Заказы (Orders)

**Модуль:** `orders`

### GET `/orders`
- **Описание:** Получить список заказов
- **Требует аутентификации:** Да
- **Возвращает:** 
  - Для администратора: все заказы
  - Для пользователя: только свои заказы
- **Возвращает:** Массив заказов с элементами заказа и товарами

### GET `/orders/:id`
- **Описание:** Получить информацию о заказе по ID
- **Требует аутентификации:** Да
- **Параметры:**
  - `id` (UUID) - ID заказа
- **Возвращает:** Информация о заказе с элементами и товарами
- **Особенности:** Пользователи могут видеть только свои заказы

### POST `/orders`
- **Описание:** Создать новый заказ
- **Требует аутентификации:** Да
- **Параметры:**
  - `deliveryAddress` (string) - Адрес доставки
  - `items` (array) - Массив элементов заказа:
    - `productId` (UUID) - ID товара
    - `quantity` (number) - Количество
    - `price` (number) - Цена за единицу
- **Возвращает:** Созданный заказ
- **Особенности:** 
  - Автоматически рассчитывает общую сумму
  - Уменьшает количество товара на складе
  - Устанавливает статус "Принят"

### PATCH `/orders/:id`
- **Описание:** Обновить заказ (статус, адрес доставки)
- **Требует аутентификации:** Да
- **Параметры:**
  - `id` (UUID) - ID заказа
  - `status` (enum, опционально) - Статус заказа (Принят, Готовится, Доставляется, Выполнен, Отменен)
  - `deliveryAddress` (string, опционально) - Адрес доставки
- **Возвращает:** Обновленный заказ
- **Особенности:** Пользователи могут обновлять только свои заказы

### PATCH `/orders/:id/cancel`
- **Описание:** Отменить заказ
- **Требует аутентификации:** Да
- **Параметры:**
  - `id` (UUID) - ID заказа
- **Возвращает:** Отмененный заказ
- **Особенности:** 
  - Возвращает товары на склад
  - Нельзя отменить выполненный заказ

### DELETE `/orders/:id`
- **Описание:** Удалить заказ
- **Требует аутентификации:** Да (только администратор)
- **Параметры:**
  - `id` (UUID) - ID заказа
- **Возвращает:** Подтверждение удаления

---

## 📊 Статистика по модулям

- **Аутентификация:** 2 функции
- **Пользователи:** 3 функции
- **Товары:** 6 функций
- **Категории:** 5 функций
- **Корзина:** 5 функций
- **Избранное:** 4 функции
- **Заказы:** 6 функций

**Всего:** 31 функция

---

## 🔑 Роли пользователей

- **Администратор** - полный доступ ко всем функциям
- **Пользователь** - доступ к своим данным и функциям покупки

---

## 📝 Примечания

1. Все эндпоинты, требующие аутентификации, используют JWT токен в заголовке `Authorization: Bearer <token>`
2. UUID параметры валидируются автоматически
3. При создании заказа автоматически проверяется наличие товара на складе
4. При отмене заказа товары автоматически возвращаются на склад
5. Все даты создаются автоматически при создании записей
6. Бонусные баллы пользователя можно начислять через обновление профиля (в будущем можно добавить отдельный эндпоинт)

